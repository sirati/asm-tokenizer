# AI GENERATED
this file was generated by AI, and I the author have not yet cleaned it up properly
## TLDR
1. install nix & enable flakes
2. use nix to install direnv and nix-direnv
3. run `direnv allow` in the project directory
4. use Zed editor as it automatically integrated with direnv

Following is AI content

# Quick Start Guide

Get up and running in 3 steps!

## One-Time System Setup

Install these tools system-wide (you only need to do this once):

### 1. Install Nix with flakes

```bash
sh <(curl -L https://nixos.org/nix/install) --daemon
```

Enable flakes in `~/.config/nix/nix.conf`:
```
experimental-features = nix-command flakes
```

### 2. Install direnv

```bash
# Using Nix (recommended)
nix profile install nixpkgs#direnv

# Or using your package manager
sudo apt install direnv      # Ubuntu/Debian
sudo dnf install direnv      # Fedora
brew install direnv          # macOS
```

### 3. Hook direnv into your shell

Add to `~/.bashrc` or `~/.zshrc`:
```bash
eval "$(direnv hook bash)"   # for bash
eval "$(direnv hook zsh)"    # for zsh
```

Then restart your shell:
```bash
source ~/.bashrc  # or ~/.zshrc
```

### 4. (Optional) Install nix-direnv for better performance

```bash
nix profile install nixpkgs#nix-direnv
```

Create `~/.config/direnv/direnvrc`:
```bash
source $HOME/.nix-direnv/direnvrc
```

## Project Setup

Once in the project directory:

```bash
cd asm-tokenizer
direnv allow
```

That's it! The environment will automatically load.

## Verify Installation

```bash
python --version              # Should show Python 3.14.x
python -c "import angr"       # Should work without errors
python -c "import pandas"     # Should work without errors
```

## Daily Usage

Just `cd` into the project directory - the environment activates automatically!

```bash
cd asm-tokenizer              # Environment loads automatically
python tokenizer/low_level.py  # Run your scripts
cd ..                         # Environment unloads automatically
```

## Common Commands

```bash
direnv allow      # Allow direnv to load .envrc (first time or after changes)
direnv reload     # Manually reload the environment
direnv status     # Check direnv status
nix develop       # Manually enter the Nix shell (alternative to direnv)
```

## Troubleshooting

**Environment blocked?**
```bash
direnv allow
```

**Environment not loading?**
```bash
direnv reload
```

**Need to clear cache?**
```bash
rm -rf ~/.cache/nix-direnv
direnv reload
```

## What You Get

- Python 3.14
- All project dependencies (angr, capstone, lief, pandas, numpy, etc.)
- Automatic environment activation/deactivation
- No virtual environments needed!
- Reproducible setup across all machines

For detailed information, see [SETUP.md](SETUP.md)
